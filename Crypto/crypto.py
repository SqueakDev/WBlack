import time
import socket
import logging
import tinydb
import json
print(f"crypto install...")

for i in range(101):
	print(f"Установка:{i}%")
	time.sleep(3)
if tinydb=="Crypto":
		tinydb.check()
elif logging=='crypto2':
			logging.run()
			def _(*args):
				for  args in range(3):
					args.open()
					key=""
					key.running
					crypto="./json/crypto.json"
					json.load(crypto)
					mongoDB=True
					mongo=mongoDB
					if __init__=='__main__':
						Server=Server("89.0.0.2",7878)
						Server.start()
